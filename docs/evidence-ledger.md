# Pangram CLI external evidence ledger

Status: non-normative research record

This ledger records mutable external facts that influence implementation.
Product contracts state required behavior; this file records why a current
external assumption was considered credible and when it must be rechecked.

| Evidence | Source | Checked | Expires or recheck gate |
| --- | --- | --- | --- |
| Pangram 4 is the current text model; unspecified requests temporarily route to Pangram 3; Pangram 3 is scheduled for deprecation on 2026-09-30; Pangram 4 API text billing is one billable unit per started 100 words (USD 0.05 per unit) | [Introducing Pangram 4](https://www.pangram.com/blog/introducing-pangram-4) | 2026-07-29 | Before public pricing docs and each public release |
| Pangram 4 text submission posts JSON to `https://text.external-api.pangram.com/task` with request field `model` set to `pangram-4`, optional `public_dashboard_link`, and `x-api-key` auth, then polls `GET /task/{id}`; a succeeded result reports version `4.0`, and every Pangram 4 window carries `is_humanized` and `humanizer_score` | [Pangram SDK v1.0.0 tag `ca42297`](https://github.com/pangramlabs/pangram-sdk/tree/ca42297) (2026-07-29) | 2026-07-31 | Before Phase 2 protocol implementation and live conformance |
| Pangram 4 segment output adds `humanizer_score` and `is_humanized`; offsets are zero-based and half-open; document classification is Human, Mixed, or AI; intended input is at least 50 words | [Pangram 4 model card](https://www.pangram.com/research/model-card/pangram-4) | 2026-07-29 | Before Phase 2 normalization and live conformance |
| The rendered legacy ReadTheDocs reference still omits Pangram 4 model selection and humanizer fields and describes the Pangram 3-era bulk rule. Pangram's current Mintlify source and SDK v1.0.0 tag supersede it for the text and bulk contracts; the rendered page is treated as stale evidence, not a protocol unknown | [legacy ReadTheDocs reference](https://pangram.readthedocs.io/en/latest/api/rest.html), [current official API source](https://github.com/pangramlabs/docs/tree/19c920036efe8344d75eb8dabba77083fda9eaf9/api-reference), and [Pangram SDK](https://github.com/pangramlabs/pangram-sdk/tree/ca42297) | 2026-07-31 | Before each public release |
| Pangram 4 bulk uses one job-wide JSON `model` field set to `pangram-4`; each valid item costs one unit per started 100-word block with a minimum of one, a request is capped at 1,000 billable units, no separate item-count limit is documented, an over-limit request returns HTTP 413, and no public-dashboard-link request or response field is documented. The 2026-08-01 Phase 3 loopback packet re-verified `eb214f4` as still the latest commit on `api-reference/bulk-api.mdx` and locked its exact wire shapes (submit `items`/`text` plus job-wide `model`, 202 accepted/failed item lists, status counters, items/results pages with offset/limit and max limit 1,000, epoch-second string timestamps, 48-hour terminal retention, and the 401/402/403/404/413/422/500/503 error matrix) in contracts.md section 9.1 | [official Bulk API source at `eb214f4`](https://github.com/pangramlabs/docs/blob/eb214f4c394979b8ac1742d42cc4709209eb14d6/api-reference/bulk-api.mdx) | 2026-08-01 | Before Phase 3 production client, live conformance, and each public release |
| Pangram Image is available in the dashboard, but Image API access is invitation-only during the research preview | [Introducing Pangram Image Detection](https://www.pangram.com/blog/introducing-pangram-image-detection) | 2026-07-29 | Image detection stays out of scope until the API is documented and generally available |
| Bulk metadata and results are retained for 48 hours after terminal state; timestamps are Unix epoch seconds encoded as strings | [official Bulk API source at `eb214f4`](https://github.com/pangramlabs/docs/blob/eb214f4c394979b8ac1742d42cc4709209eb14d6/api-reference/bulk-api.mdx) | 2026-07-31 | Before Phase 3 and each public release |
| The current official plagiarism reference defines `plagiarized_sentences` as an integer count, matching the canonical contract; a one-block synthetic live response confirmed the integer on 2026-08-24 | [official plagiarism reference at `19c9200`](https://github.com/pangramlabs/docs/blob/19c920036efe8344d75eb8dabba77083fda9eaf9/api-reference/plagiarism-detection.mdx) plus the sanitized Phase 7 live shape | 2026-08-24 | On upstream plagiarism contract changes |
| Pangram's current general credit contract charges 5 credits per plagiarism scan. The Developer API plagiarism reference points to Developer pricing but does not state a separate plagiarism rate; the Developer pricing surface states only AI detection at USD 0.05 per 100 words. Phase 7 therefore uses 5 units as a conservative plagiarism ceiling estimate and sums it with AI-detection units for combined analysis | [AI detection credit](https://www.pangram.com/knowledge-hub/what-is-an-ai-detection-credit), [plagiarism API reference](https://docs.pangram.com/api-reference/plagiarism-detection), [Developer API pricing](https://www.pangram.com/solutions/api) | 2026-08-24 | On any Pangram plagiarism or Developer API pricing change; before public release |
| Official file-upload sources conflicted; minimal private RTF, PDF, and DOCX live responses on 2026-08-24 returned the SDK's rich ordered array with extracted text, predictions, counts, and windows, while omitting Pangram 4 humanizer fields and public links | [official file reference at `19c9200`](https://github.com/pangramlabs/docs/blob/19c920036efe8344d75eb8dabba77083fda9eaf9/api-reference/file-external.mdx), [Pangram SDK v1.0.0](https://github.com/pangramlabs/pangram-sdk/blob/ca42297609e65b1474be168901168ef4ed7a2950/README.md#L80-L116), and the sanitized Phase 7 live shapes | 2026-08-24 | On upstream file contract changes |
| Rust target runtime baselines | [Rust platform support](https://doc.rust-lang.org/rustc/platform-support.html) | 2026-07-27 | On MSRV selection and before each public release |
| MCP stateless lifecycle, deprecated Roots, result metadata, and extension model | [MCP 2026-07-28 changelog](https://modelcontextprotocol.io/specification/2026-07-28/changelog) | 2026-07-28 | Before Phase 6 MCP implementation and public docs |
| The Tasks extension repository remains experimental and has no release | [MCP Tasks extension](https://github.com/modelcontextprotocol/ext-tasks) | 2026-07-28 | Before considering the extension for a later product revision |
| Stable RMCP 3.1.2 supports the Phase 6 MCP implementation and requires Rust 1.88. The exact upstream source commit is `02c62aef2e331e5cf79c06c744eb1eb052cc8ebd`; the downloaded crate SHA-256 is `c8dddc5b1924b9a59fba420166160ca2c4663a4e01803e52eda33070f56d63c8`. Crate metadata is Apache-2.0 and the upstream license transition notice is retained as dependency evidence | [RMCP 3.1.2 source commit](https://github.com/modelcontextprotocol/rust-sdk/commit/02c62aef2e331e5cf79c06c744eb1eb052cc8ebd), [RMCP 3.1.2 crate documentation](https://docs.rs/crate/rmcp/3.1.2) | 2026-08-12 | On RMCP version, source, checksum, license, or feature changes; before each public release |
| Phase 6 capability-root dependencies `cap-std 4.0.2` and `cap-fs-ext 4.0.2` are licensed under `Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT`, require Rust 1.70, and resolve `winx 0.36.4` under the `Apache-2.0 WITH LLVM-exception` SPDX expression. The exact expression is admitted to the authoritative cargo-deny allow-list; no broader license family is allowed | locked crate Cargo.toml metadata and `cargo deny check licenses` output against the Phase 6 lockfile | 2026-08-13 | On `cap-std`, `cap-fs-ext`, `cap-primitives`, or `winx` version or license changes; before each public release |
| The official MCP conformance applicability reference is pinned to `@modelcontextprotocol/conformance` 0.2.0-alpha.11, npm `gitHead` `c321dd32035556e6769d3724a8ee97d87c3faaac`, and integrity `sha512-imPK9tx5gQsL6ZKQq4MrsyDYfSaIwpRmX6+ogjbeAXs9LGvxkBxWcY7KcS7TvwaBk/ZiVWl6b/naF4q83UwDRA==`. The suite cannot drive stdio while issue 258 remains open. Its frozen `2026-07-28` server requirements also mandate a full diagnostic server including `test_*` tools, prompts, templates, binary resources, completion, SSE, DNS-rebinding checks, sampling, elicitation, roots, and input-required flows rather than selecting scenarios from advertised capabilities. Individual overlapping HTTP scenarios could exercise shared handler code, but a broader fixture would prove neither Pangram's full-profile nor stdio conformance. Phase 6 uses compiled stdio product tests and records the official suite as currently inapplicable | [conformance source commit](https://github.com/modelcontextprotocol/conformance/commit/c321dd32035556e6769d3724a8ee97d87c3faaac), [frozen 2026-07-28 server requirements](https://github.com/modelcontextprotocol/conformance/blob/c321dd32035556e6769d3724a8ee97d87c3faaac/requirements/2026-07-28.yaml), [stdio support issue 258](https://github.com/modelcontextprotocol/conformance/issues/258) | 2026-08-13 | On conformance package version or integrity changes; when issue 258 or capability-aware selection changes; before each public release |
| Terminal Control package version and native-platform support | Package registry and upstream repository | 2026-07-23 | Immediately before Phase 5 scaffolding |
| Rust stable toolchain | [Rust stable channel manifest](https://static.rust-lang.org/dist/channel-rust-stable.toml) | 2026-07-28: 1.97.1 | Before each implementation phase and public release |
| Phase 0 dependency versions and declared MSRV compatibility | crates.io API metadata and deps.dev | 2026-07-28 | On direct dependency changes; prove the selected lockfile on Rust 1.85 before accepting the manifest |
| Phase 1 dependency versions and declared MSRV compatibility: `directories 6.0.0`, `rpassword 7.5.4`, `secrecy 0.10.3`, `toml 1.0.7`, `zeroize 1.8.2`, `windows-sys 0.61.2` (Windows-only), and dev dependency `tempfile 3.27.0` | crates.io API metadata and deps.dev | 2026-07-30 | On direct dependency changes; prove the selected lockfile on Rust 1.85 before accepting the manifest |
| `option-ext 0.2.0` is licensed MPL-2.0 (Mozilla Public License 2.0). It is a transitive, non-Windows build dependency reached through `microck-pangram-cli 0.1.0 -> directories 6.0.0 -> dirs-sys 0.5.0 -> option-ext 0.2.0`. MPL-2.0 is OSI-approved, FSF Free/Libre, file-level weak copyleft, and compatible with this project's MIT outbound license. The operator decision to allow MPL-2.0 was recorded 2026-07-30; the authoritative allow-list is the cargo-deny `[licenses] allow` entry in `.github/workflows/ci.yml` | `option-ext 0.2.0` Cargo.toml metadata and cargo-deny rejection output | 2026-07-30 | On `directories` or `dirs-sys` version changes; on any `option-ext` version change; before each public release |
| Phase 2 analysis dependency versions and declared MSRV compatibility: `tokio 1.53.1` (declared rust-version 1.71, MIT), `tokio-util 0.7.19` (declared rust-version 1.71, MIT), `reqwest 0.13.4` (declared rust-version 1.85, MIT OR Apache-2.0), `url 2.5.8` (MIT OR Apache-2.0), and dev-only `axum 0.8.9` (declared rust-version 1.80, MIT). Reqwest exact feature names verified against the 0.13.4 manifest: `default-features = false` plus `rustls` (rustls-platform-verifier; no native TLS compiled), `system-proxy` (documented system proxy discovery), `http2`, `charset`, `json`, `multipart`, `gzip`, `brotli`, `deflate`, `stream`. Phase 7 adds `multipart`, resolving `mime_guess 2.0.5` and `unicase 2.9.0`, for the verified file endpoint contract | crates.io API metadata via `cargo info <crate>@<version>` and the Phase 7 locked graph | 2026-08-24 | On direct dependency changes; prove the selected lockfile on the package MSRV before accepting the manifest |
| Phase 2 output-projection dependency and MSRV supersession: `toon-format = 0.5.0` (MIT, edition 2021, no declared `rust-version`) enters with `default-features = false` so the heavy `cli` feature (clap/anyhow/tiktoken-rs/comfy-table/ratatui/crossterm/tui-textarea/arboard/syntect/unicode-width/chrono) is never compiled. `toon_format::encode_default(&serde_json::Value) -> ToonResult<String>` and `toon_format::decode_default::<T: serde::de::DeserializeOwned>(&str) -> ToonResult<T>` are the exact public API. `toon-format 0.5.0` requires Rust 1.87.0: `src/decode/parser.rs:1497` calls `u64::is_multiple_of`, belonging to `unsigned_is_multiple_of` which stabilized in Rust 1.87.0 (rust-lang/rust#128101), and the crate fails to compile on Rust 1.85.0 with `E0658` (`use of unstable library feature unsigned_is_multiple_of`); the crate is a single library target with no feature flag that excludes the decoder, so the encode-only projection cannot avoid the floor. The operator/orchestrator decision recorded 2026-07-31 selects Option 1: because the locked v1 projection contract includes TOON and the architecture makes the lowest selected direct-dependency-compatible toolchain the package `rust-version`, MSRV rises from 1.85 to the exact minimum 1.87.0 (not preemptively to the RMCP 1.88 prerelease floor). Verified: builds cleanly on Rust 1.87.0 and current stable 1.97.1. This supersedes the recheck cadence of the earlier rows: on direct dependency changes, prove the selected lockfile on Rust 1.87 before accepting the manifest. | `cargo info toon-format@0.5.0`, `cargo +1.85.0 build` E0658 output, `cargo +1.87.0 build` success | 2026-07-31 | On `toon-format` version change; on any direct dependency change that could lower or raise the floor; before each public release |
| Phase 2 license allowances beyond the Phase 1 set, verified against the locked dependency graph with `cargo deny check licenses`: **ISC** (`rustls-webpki 0.103.13`, the webpki certificate-verification library; `aws-lc-rs 1.17.3` and `aws-lc-sys 0.43.0` offer ISC as a primary/alternative SPDX term); **BSD-3-Clause** (`alloc-no-stdlib 2.0.4`, `alloc-stdlib 0.2.4`, and `brotli 8.0.4` reached through `tower-http` decompression used by the reqwest compression features; also an aws-lc-sys SPDX conjunction term); **MIT-0** (appears only inside the aws-lc-sys `(Apache-2.0 OR ISC OR MIT-0)` SPDX alternative); **CDLA-Permissive-2.0** (`webpki-root-certs 1.0.9`, Mozilla's root certificate data pack pulled by rustls-platform-verifier). All are OSI/FSF-approved permissive licenses compatible with this project's MIT outbound license; they extend the authoritative cargo-deny `[licenses] allow` entry in `.github/workflows/ci.yml` without loosening any other policy | `cargo deny check licenses` rejection output against the Phase 2 lockfile; each crate's Cargo.toml `license` field | 2026-07-31 | On rustls, rustls-platform-verifier, rustls-webpki, aws-lc-rs/-sys, brotli, or webpki-root-certs version changes; before each public release |
| Phase 4 history dependency versions and declared MSRV compatibility: `rusqlite = "=0.39.0"` (MIT, edition 2021, no declared `rust-version`; bundled crates list no MSRV beyond "latest stable at time of release"), `default-features = false` plus `features = ["bundled"]`. `default-features = false` excludes rusqlite 0.39.0's only default feature `cache` (which pulls `hashlink`, MSRV 1.85), keeping the smallest graph that satisfies architecture-spec 11.1 (`bundled` + FTS5 + transactions). The newer `rusqlite 0.40.1` (and sys-only `libsqlite3-sys 0.38.1`) pair was rejected: stable `cfg_select!` (macro stabilized in Rust 1.95) is used in rusqlite 0.40.x source and in the libsqlite3-sys 0.38.1 `build.rs`, so those releases effectively require Rust 1.95, above the locked package MSRV 1.87. rusqlite 0.39.0 requires `libsqlite3-sys ^0.37.0`; under Cargo's 0.x caret semantics `^0.37.0` resolves only to `>=0.37.0, <0.38.0`, so the incompatible 0.38.1 can never be selected through the declared range, and Cargo.lock additionally pins the transitive crate at `0.37.0`, making a direct sys dependency unnecessary. `libsqlite3-sys 0.37.0` (MIT, no declared `rust-version`) `build.rs::build_bundled::main` unconditionally passes `-DSQLITE_ENABLE_FTS5`, alongside FTS3, RTREE, JSON1, `SQLITE_THREADSAFE=1`, `SQLITE_USE_URI`, and `SQLITE_DEFAULT_FOREIGN_KEYS=1`. The bundled vendored amalgamation is SQLite 3.51.3. Embedded FTS5 sources (`sqlite3/sqlite3.c` FTS5 translation units, `fts5.h` in `sqlite3/sqlite3.h`) ship with the sys crate. Direct transitive additions: `bitflags 2.x` (MSRV 1.56), `smallvec 1.x` (no floor), `fallible-iterator 0.3.0` (no floor), `fallible-streaming-iterator 0.1.9` (no floor); build-only `cc 1.x` (MSRV 1.63) and target-gated `pkg-config 0.3.x` plus Windows-only `vcpkg`. No `hashlink`, no async database/repository/migration-framework dependencies. FTS5 runtime proof: `PRAGMA compile_options` against the compiled library returns `ENABLE_FTS5` and `DEFAULT_FOREIGN_KEYS`, `SELECT sqlite_version()` returns `3.51.3`, and the contracted `CREATE VIRTUAL TABLE analysis_search USING fts5(... tokenize = 'unicode61')` statement executes (tests/history-sqlite-baseline.rs). Compiled validation VERIFIED 2026-08-02 on a separate disposable Crabbox box on Oracle Paris (the `oracle-paris` VPS `100.96.124.15` via Tailscale, external `crabbox-paris-provider.sh`, lease `cbx_d30368ca0f82`, slug `pangram-p4-sqlite-a73kf`, container `6645f5f9c9d9`, `crabbox:full`, aarch64 Ubuntu 22.04.5; never the local Docker host, `static_yoga`, or `remoteuse-t7-profiles-policy`): Cargo's `generate-lockfile` confirmed the regenerated `Cargo.lock` is idempotent-consistent after the direct `libsqlite3-sys` edge removal with `rusqlite` as the only root edge, the focused probe passes 3/3 on both current stable 1.97.1 and Rust 1.87.0 (a standalone compiled run additionally printed `sqlite_version()=3.51.3` with `ENABLE_FTS5`/`ENABLE_FTS3`/`ENABLE_RTREE`/`DEFAULT_FOREIGN_KEYS`/`THREADSAFE=1`), `cargo build --locked --all-features` passes under 1.87.0, and `cargo fmt --check` / `cargo test --locked --all-features` (400 passed, 20 binaries) / `cargo clippy --locked --all-features --all-targets -- -D warnings` all pass under 1.97.1 | [rusqlite 0.39.0 crate source](https://crates.io/crates/rusqlite/0.39.0), [libsqlite3-sys 0.37.0 crate source](https://crates.io/crates/libsqlite3-sys/0.37.0), [rusqlite repository](https://github.com/rusqlite/rusqlite), crates.io API metadata | 2026-08-01 | On `rusqlite`/`libsqlite3-sys` version or feature changes; on any direct dependency change; before each public release |
| Phase 4 license review for the SQLite baseline: `rusqlite 0.39.0` and `libsqlite3-sys 0.37.0` are MIT (Cargo.toml `license = "MIT"`). The vendored SQLite amalgamation is public domain per [SQLite copyright](https://www.sqlite.org/copyright.html) and both crates' READMEs, has no bearing on the crates' MIT license, and introduces no new SPDX identifier into the cargo-deny graph. VERIFIED 2026-08-02 on the disposable Oracle Paris box (external provider, lease `cbx_d30368ca0f82`, slug `pangram-p4-sqlite-a73kf`): `cargo deny check licenses --config <authoritative CI allow-list>` against the regenerated Phase 4 lockfile reports `licenses ok` with no new allow entries required, and `cargo audit` against the same lockfile reports no vulnerabilities across 323 dependencies. All other direct transitive additions (`bitflags`, `smallvec`, `fallible-iterator`, `fallible-streaming-iterator`, `cc`, `pkg-config`) carry licenses already admitted by the authoritative allow-list (MIT / Apache-2.0 / Unicode-3.0 / etc.) and required no new allow entries | vendored crate Cargo.toml metadata; `cargo deny check licenses` output against the Phase 4 lockfile | 2026-08-01 | On `rusqlite`, `libsqlite3-sys`, or any transitive version change; before each public release |
| Phase 8 documentation stack: `fumadocs-core 16.15.1`, `fumadocs-ui 16.15.1`, `fumadocs-mdx 15.3.1`, `next 16.3.2`, `react 19.2.8`, `react-dom 19.2.8`, and `typescript 6.0.3`. Fumadocs Core and UI require Next 16 and React 19.2; Fumadocs MDX accepts Next 15.3 or 16 and Fumadocs Core 16.7 or newer. TypeScript stays on the exact 6.0.3 line used by Fumadocs' current `next-min` example instead of adopting TypeScript 7 before upstream demonstrates it. Tailwind and PostCSS match that example at `tailwindcss 4.3.3`, `@tailwindcss/postcss 4.3.3`, and `postcss 8.5.26` | [Fumadocs Next.js manual installation](https://www.fumadocs.dev/docs/manual-installation/next), [current Fumadocs `next-min` example](https://github.com/fuma-nama/fumadocs/tree/main/examples/next-min), npm registry peer and engine metadata | 2026-08-25 | On any direct docs dependency change; before each public docs deployment |
| Phase 8 updater dependencies are pinned by the lockfile at `base64 0.23.1`, `ed25519-dalek 3.0.0`, `semver 1.0.28`, `tar 0.4.46`, `xz2 0.1.7`, and `zip 8.4.0`; completion generation uses `clap_complete 4.6.9`. The complete locked graph passes Rust 1.88 and current Rust 1.97.1. The selected libraries own encoding, strict Ed25519 verification, SemVer ordering, and the two contracted archive formats; product code owns trust order, closed manifest values, archive layout, receipt ownership, and replacement policy | crates.io package metadata, locked crate manifests, and the remote current/MSRV all-feature suites | 2026-08-25 | On any updater dependency or feature change; before signing a public manifest |
| Tegami `1.4.0` is pinned exactly and requires Node.js 24. Its CLI exposes separate `version`, `publish`, dry-run publish, and cleanup operations; the GitHub plugin is configured with `createTags: false` and `release: false`, leaving native tags and releases to release CI. The repository uses a manual version-PR workflow and a separate environment-gated npm trusted-publishing workflow | [Tegami documentation](https://tegami.fuma-nama.dev/), exact installed 1.4.0 package source and CLI output | 2026-08-25 | On Tegami version, Node floor, registry ownership, or publication-workflow changes |
| cargo-dist `0.32.0` is pinned for release version and target planning. The single Cargo package exposes feature-gated development binaries that cargo-dist includes in its default archive plan, so the repository's deterministic packager owns the one public archive layout while cargo-dist remains the target-plan validator. cargo-about `0.9.2` generates the committed-policy third-party notice and the pinned Anchore action writes the SPDX JSON into the release asset directory before checksums and provenance | [cargo-dist 0.32.0 release](https://github.com/axodotdev/cargo-dist/releases/tag/v0.32.0), [cargo-about 0.9.2 release](https://github.com/EmbarkStudios/cargo-about/releases/tag/0.9.2), and the pinned Anchore action input contract | 2026-08-25 | On release-tool, target, archive, SBOM, or notice-policy changes |
| Registry names, package-manager formulas, and client configuration formats | Each registry or client owner | Not yet revalidated | Before implementing or publishing that channel |
| Search-positioning volumes | Semrush US database | 2026-07-23 research snapshot only | Before public landing-page copy; advisory only |

An expired or unverified row cannot justify a public compatibility, price,
retention, platform, or availability claim. Revalidation updates this ledger,
then changes a normative contract only when external behavior actually changed.
